import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      light: '#FBC26C',
      main: '#FBB348',
      dark: '#AF7D32',
    },
  },
  typography: {
    fontFamily: 'Comfortaa, sans-serif',
  },
});